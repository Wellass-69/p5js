function setup() {
  createCanvas(500,  500);
   background("turquoise");
}

function draw() {
  stroke("black") //borda;
 fill("red");
 //console.log(mouseIsPressed);
 
  if(mouseIsPressed);
   square(mouseX,mouseY,90,90)
}
